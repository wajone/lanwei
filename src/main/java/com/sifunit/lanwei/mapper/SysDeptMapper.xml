<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sifunit.lanwei.mapper.SysDeptMapper">
  <resultMap id="BaseResultMap" type="com.sifunit.lanwei.domain.SysDept">
    <constructor>
      <idArg column="dept_id" javaType="java.lang.Long" jdbcType="BIGINT" />
      <arg column="dept_name" javaType="java.lang.String" jdbcType="VARCHAR" />
      <arg column="dept_parent_id" javaType="java.lang.Long" jdbcType="BIGINT" />
      <arg column="dept_parent_name" javaType="java.lang.String" jdbcType="VARCHAR" />
      <arg column="dept_desc" javaType="java.lang.String" jdbcType="VARCHAR" />
      <arg column="flag" javaType="java.lang.Boolean" jdbcType="BIT" />
      <arg column="create_time" javaType="java.util.Date" jdbcType="TIMESTAMP" />
      <arg column="update_time" javaType="java.util.Date" jdbcType="TIMESTAMP" />
    </constructor>
  </resultMap>
  <sql id="Base_Column_List">
    dept_id, dept_name, dept_parent_id, dept_parent_name, dept_desc, flag, create_time, 
    update_time
  </sql>
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from sys_dept
    where dept_id = #{deptId,jdbcType=BIGINT}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from sys_dept
    where dept_id = #{deptId,jdbcType=BIGINT}
  </delete>
  <insert id="insert" parameterType="com.sifunit.lanwei.domain.SysDept">
    insert into sys_dept (dept_id, dept_name, dept_parent_id, 
      dept_parent_name, dept_desc, flag, 
      create_time, update_time)
    values (#{deptId,jdbcType=BIGINT}, #{deptName,jdbcType=VARCHAR}, #{deptParentId,jdbcType=BIGINT}, 
      #{deptParentName,jdbcType=VARCHAR}, #{deptDesc,jdbcType=VARCHAR}, #{flag,jdbcType=BIT}, 
      #{createTime,jdbcType=TIMESTAMP}, #{updateTime,jdbcType=TIMESTAMP})
  </insert>
  <insert id="insertSelective" parameterType="com.sifunit.lanwei.domain.SysDept">
    insert into sys_dept
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="deptId != null">
        dept_id,
      </if>
      <if test="deptName != null">
        dept_name,
      </if>
      <if test="deptParentId != null">
        dept_parent_id,
      </if>
      <if test="deptParentName != null">
        dept_parent_name,
      </if>
      <if test="deptDesc != null">
        dept_desc,
      </if>
      <if test="flag != null">
        flag,
      </if>
        create_time,
        update_time,

    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="deptId != null">
        #{deptId,jdbcType=BIGINT},
      </if>
      <if test="deptName != null">
        #{deptName,jdbcType=VARCHAR},
      </if>
      <if test="deptParentId != null">
        #{deptParentId,jdbcType=BIGINT},
      </if>
      <if test="deptParentName != null">
        #{deptParentName,jdbcType=VARCHAR},
      </if>
      <if test="deptDesc != null">
        #{deptDesc,jdbcType=VARCHAR},
      </if>
      <if test="flag != null">
        #{flag,jdbcType=BIT},
      </if>
      now(),now(),
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.sifunit.lanwei.domain.SysDept">
    update sys_dept
    <set>
      <if test="deptName != null">
        dept_name = #{deptName,jdbcType=VARCHAR},
      </if>
      <if test="deptParentId != null">
        dept_parent_id = #{deptParentId,jdbcType=BIGINT},
      </if>
      <if test="deptParentName != null">
        dept_parent_name = #{deptParentName,jdbcType=VARCHAR},
      </if>
      <if test="deptDesc != null">
        dept_desc = #{deptDesc,jdbcType=VARCHAR},
      </if>
      <if test="flag != null">
        flag = #{flag,jdbcType=BIT},
      </if>
      <if test="createTime != null">
        create_time = #{createTime,jdbcType=TIMESTAMP},
      </if>

        update_time = now(),

    </set>
    where dept_id = #{deptId,jdbcType=BIGINT}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.sifunit.lanwei.domain.SysDept">
    update sys_dept
    set dept_name = #{deptName,jdbcType=VARCHAR},
      dept_parent_id = #{deptParentId,jdbcType=BIGINT},
      dept_parent_name = #{deptParentName,jdbcType=VARCHAR},
      dept_desc = #{deptDesc,jdbcType=VARCHAR},
      flag = #{flag,jdbcType=BIT},
      create_time = #{createTime,jdbcType=TIMESTAMP},
      update_time = #{updateTime,jdbcType=TIMESTAMP}
    where dept_id = #{deptId,jdbcType=BIGINT}
  </update>
  <update id="updateByIds">
    update sys_dept set flag=0 where dept_id in
    <foreach collection="list" open="(" separator="," close=")" item="id">
      #{id}
    </foreach>
  </update>
  <select id="list" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from sys_dept
  </select>
  <select id="selectCountById" resultType="long">
    select count(*) from sys_dept where flag=1 and DEPT_PARENT_ID = #{deptId}
  </select>

  <select id="selectCountByIds" resultType="long">
    select count(*) from sys_dept where flag=1 and DEPT_PARENT_ID in
    <foreach collection="list" open="(" separator="," close=")" item="id">
      #{id}
    </foreach>
  </select>

  <resultMap type="com.sifunit.lanwei.domain.LayuiTree" id="parentTreeMap">
    <id column="dept_id" property="id"/>
    <result column="dept_parent_id" property="parent_id"/>
    <result column="dept_name" property="title"/>
    <collection property="children" ofType="com.sifunit.lanwei.domain.LayuiTree" column="dept_id" select="getDeptChildren"/>
  </resultMap>

  <!-- 先查询菜单根级目录 -->
  <!-- 这里的返回结果必须为resultMap，并且值为上面构建的resultMap的id的值 -->
  <select id="findAllRecursion" resultMap="parentTreeMap">
    SELECT dept_id,dept_parent_id,dept_name FROM sys_dept  WHERE  dept_parent_id=0
  </select>
  <!-- 再利用上次查询结果colliection中column的值cid做递归查询，查出所有子菜单 -->
  <!-- 这里的返回结果必须为resultMap，并且值为上面构建的resultMap的id的值 -->
  <select id="getDeptChildren" resultMap="parentTreeMap">
    SELECT
      dept_id,dept_parent_id,dept_name
    FROM
      sys_dept
    WHERE
      dept_parent_id= #{id}
  </select>
</mapper>